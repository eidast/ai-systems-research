# TypeScript/Node — mem-large-json-transform

## Entry point
- `run.sh`
- `transform.mjs`

## Notes
- Node standard-library JSON parsing and aggregation
- Dataset path is passed as the first argument
- Produces a deterministic JSON summary
