# Benchmarks Workspace

Benchmark definitions are grouped by workload class:

- `cpu-bound/`
- `memory-bound/`
- `parallelism/`
- `concurrency/`
- `io-bound/`

Language-specific implementations should be linked conceptually to these benchmark definitions.

See [Benchmark Suite v1](../docs/benchmark-suite.md).
