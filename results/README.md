# Results Workspace

This directory stores benchmark outputs.

## Structure

- `raw/` — original measurement outputs
- `processed/` — cleaned and normalized data
- `charts/` — generated visualizations

## Rule

Never overwrite raw benchmark outputs without preserving provenance.
